#include <stdio.h>
#include <string.h>

int main()
{
   char nome[30];
   char cidade[30];
  

   strcpy(nome, "Mariana");
   strcpy(cidade, "Campinas");

   puts(nome);
   puts(cidade);
   
   return 0;
}
